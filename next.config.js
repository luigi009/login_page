// @ts-check

/**
 * @type {import('next').NextConfig}
 **/
const nextConfig = {
  /* config options here */
};

module.exports = {
  images: {
    domains: ["avatars.githubusercontent.com"],
  },
};
